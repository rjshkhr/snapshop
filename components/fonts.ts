import { Eczar, Inter, Norican } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })

export const norican = Norican({
  weight: ['400'],
  subsets: ['latin']
})

export const eczar = Eczar({ subsets: ['latin'] })
